// build.gradle.kts (Project-level)

plugins {
    id("com.android.application") version "8.7.2" apply false // Apply Android plugin for application
    kotlin("android") version "2.0.0" apply false // Apply Kotlin plugin
    kotlin("plugin.compose") version "2.0.0" apply false // Apply Compose plugin (if you're using Jetpack Compose)
}

allprojects {
    repositories {

    }
}
